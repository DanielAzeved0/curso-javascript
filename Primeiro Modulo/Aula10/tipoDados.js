const nome ='Daniel';
const nome1 ="Daniel"; //Strings
const nome2 = `Daniel`;

const num = 10;
const num1 = 10.4; //Numbers

let nomeAluno; //Undefined = não aponta para nenhum local na memória
const sobrenomeAluno = null; // Null = não aponta para nenhum local na memória, mas pretende fazer isso em algum momento

const alunoAprovado = true; //true se passou na prova, ou false se não passou

 
console.log(nome, nome1, nome2);
console.log(num, num1);
console.log(nomeAluno);
console.log(sobrenomeAluno);
console.log(alunoAprovado);
